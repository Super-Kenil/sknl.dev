---
import {Icon} from 'astro-icon/components';

const {className = ''} = Astro.props;
---

<a href='/' class={`link link-hover flex items-center gap-x-1 text-sm${className!== '' ? ` ${className}` : ''}`}>
  <Icon name='lucide:arrow-left' size={16} /> Back
</a>
