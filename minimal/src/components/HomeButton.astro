---
import {Icon} from 'astro-icon/components';

const {className = ''} = Astro.props;
---

<a href='/' class={`btn${className!== '' ? ` ${className}` : ''}`}>
  <Icon name='lucide:arrow-left' size={24} /> Back
</a>
