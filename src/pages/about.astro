---
import {render} from 'astro:content';
import HomeButton from '../components/HomeButton.astro';
import BaseLayout from '../layouts/BaseLayout.astro';
import CenteredLayout from '../layouts/CenteredLayout.astro';
import {getEntry} from 'astro:content';

const about = await getEntry('about', 'about');
if (!about) {
  throw new Error('Could not find about entry');
}
const {Content: AboutContent} = await render(about);
---

<BaseLayout title='About | Super Kenil' description='The story of Kenil Sudani - from a small village in Gujarat to SDE'>
  <CenteredLayout>
    <div class='card card-border'>
      <div class='card-body'>
        <HomeButton className='mb-2' />
        <h1 class='card-title font-extrabold text-2xl'>Kenil Sudani</h1>
        <span class='font-extrabold text-lg block mt-4' aria-label='about me'>The Story</span>
        <div class='prose prose-sm' transition:animate="fade">
          <AboutContent />
        </div>
      </div>
    </div>
  </CenteredLayout>
</BaseLayout>
